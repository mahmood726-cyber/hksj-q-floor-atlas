# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# R
.Rhistory
.RData
.Rproj.user/
renv/library/
renv/staging/
renv/local/

# OS
.DS_Store
Thumbs.db

# Project-local session state (per operating rules)
PROGRESS.md
.progress_*.json

# Large parquet inputs: the canonical input parquet IS versioned (SHA-pinned in spec.md §1.5);
# future / intermediate parquets are NOT.
data/inputs/*.parquet
!data/inputs/full_method_results.parquet
!data/inputs/full_method_results.sha256

# Build / dist
dist/
build/

# Derived reference (regenerated by scripts/reference_compute.R)
tests/fixtures/golden_mas_reference.json

# Notebook + scratch
*.ipynb_checkpoints/
scratch/

# Derived compute outputs (large binary; not committed)
outputs/*.parquet
